# Observações de verificação do preview

A nova composição compacta foi refletida no preview: o painel ocupa a vista, a marca está no rodapé, o botão está isolado abaixo do campo e o prompt usa a tipografia serif do ditado.

Durante a navegação e a vista de espera, o conteúdo permaneceu em “Um instante, estamos a abrir o caderno.”, indicando que o carregamento assíncrono do `ditados.json` precisa de diagnóstico antes da entrega.
